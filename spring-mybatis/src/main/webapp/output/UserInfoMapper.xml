<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.silence.dao.UserInfoMapper">
	

	<resultMap id="userInfoMap" type="com.silence.domain.UserInfo">
        <id column="id" property="id"/>
                                                        
        <result column="{{name}}" property="{{name}}"/>
                                            
        <result column="{{password}}" property="{{password}}"/>
                                            
        <result column="{{status}}" property="{{status}}"/>
                                            
        <result column="{{type}}" property="{{type}}"/>
                                            
        <result column="{{createTime}}" property="{{create_time}}"/>
                                            
        <result column="{{updateTime}}" property="{{update_time}}"/>
                            
    </resultMap>


    <insert id="save" parameterType="com.silence.domain.UserInfo">
        <selectKey resultType="Long" keyProperty="id" order="AFTER">
            SELECT LAST_INSERT_ID()
        </selectKey>
        insert into <include refid="TABLE"/>
        (
            `id`,`name`,`password`,`status`,`type`,`create_time`,`update_time`
        )
        values
        (
            #{id},#{name},#{password},#{status},#{type},#{createTime},#{updateTime}
        )
    </insert>

    <insert id="update" parameterType="com.silence.domain.UserInfo">
        UPDATE <include refid="TABLE"/>
        <include refid="UPDATE_COLUMN_SET"/>
        <![CDATA[WHERE id = #{id}]]>
    </insert>

    <update id="delete" parameterType="java.lang.Long" >
        DELETE FROM <include refid="TABLE"/> WHERE id = #{id}
    </update>

    <!-- 查询 -->
    <select id="selectList" parameterType="java.util.Map" resultMap="userInfoMap">
        SELECT
            <include refid="QUERY_COLUMN_LIST"/>
        FROM
        <include refid="TABLE"/>
        <include refid="QUERY_WHERE_CLAUSE"/>
        <include refid="QUERY_ORDER_LIMIT_CONDITION"/>
    </select>

    <!-- batch查询 -->
    <select id="selectByBatchIds" parameterType="java.util.Map" resultMap="userInfoMap">
        SELECT
        <include refid="QUERY_COLUMN_LIST"/>
        FROM
        <include refid="TABLE"/>
        WHERE id IN
        <foreach collection="list" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </select>

    <!-- 查询总数 -->
    <select id="selectListCount" parameterType="java.util.Map" resultType="int">
        SELECT COUNT(id) AS COUNT FROM
        <include refid="TABLE"/>
        <include refid="QUERY_WHERE_CLAUSE"/>
        <include refid="QUERY_ORDER_LIMIT_CONDITION"/>
    </select>

    <!-- 查询ById -->
    <select id="selectById" parameterType="java.lang.Long" resultMap="userInfoMap">
        SELECT
        <include refid="QUERY_COLUMN_LIST"/>
        FROM
        <include refid="TABLE"/>
        WHERE id = #{id}
    </select>


	<!-- 所有查询列 -->
	<sql id="QUERY_COLUMN_LIST">
		<![CDATA[
            `id` AS 'id',`name` AS 'name',`password` AS 'password',`status` AS 'status',`type` AS 'type',`create_time` AS 'createTime',`update_time` AS 'updateTime'
		]]>
	</sql>

	<!-- 表-->
	<sql id="TABLE"><![CDATA[ user_info ]]></sql>
	
	<!-- 全部条件(更多功能可以通过queryData扩展实现)  -->
	<sql id="QUERY_WHERE_CLAUSE">
		<where>

            <if test="id != null and id != ''"><![CDATA[AND `id` = #{id}]]></if>
            <if test="name != null and name != ''"><![CDATA[AND `name` = #{name}]]></if>
            <if test="password != null and password != ''"><![CDATA[AND `password` = #{password}]]></if>
            <if test="status != null and status != ''"><![CDATA[AND `status` = #{status}]]></if>
            <if test="type != null and type != ''"><![CDATA[AND `type` = #{type}]]></if>
            <if test="createTime != null and createTime != ''"><![CDATA[AND `create_time` = #{createTime}]]></if>
            <if test="updateTime != null and updateTime != ''"><![CDATA[AND `update_time` = #{updateTime}]]></if>
            
        </where>
	</sql>
	
	<!-- 智能排序与分页 -->
	<sql id="QUERY_ORDER_LIMIT_CONDITION">
		<if test="orderField != null and orderField != '' and orderFieldType != null and orderFieldType != ''"><![CDATA[ORDER BY ${orderField} ${orderFieldType}]]></if>
		<if test="startIndex != null and startIndex &gt;= 0 and pageSize != null and pageSize &gt; 0"><![CDATA[LIMIT #{startIndex},#{pageSize}]]></if>
	</sql>

	<!-- 更新列字段,只要不为NULL则更新,除开主键列 -->
	<sql id="UPDATE_COLUMN_SET">
		<set>
                                                        
            <if test="name != null"><![CDATA[ `name` = #{name},]]></if>
                                            
            <if test="password != null"><![CDATA[ `password` = #{password},]]></if>
                                            
            <if test="status != null"><![CDATA[ `status` = #{status},]]></if>
                                            
            <if test="type != null"><![CDATA[ `type` = #{type},]]></if>
                                            
            <if test="createTime != null"><![CDATA[ `create_time` = #{createTime},]]></if>
                                            
            <if test="updateTime != null"><![CDATA[ `update_time` = #{updateTime},]]></if>
                            
		</set>
	</sql>
	
	
</mapper>